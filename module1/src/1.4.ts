{// data types in typescript

    // basic data types
    // number
    let serial: number = 10;

    // string
    let userName: string = "Joujoniki Asa Roy";

    // boolean
    let isMarried: boolean = true;

    // array
    let siblings: string[] = ['sib1', 'sib2', 'sib3'];

    // tuple
    let siblingsInfo: [string, string, string] = ['sib1', 'sib2', 'sib3'];

    // null
    let salaryRange: null = null;

    // undefined
    let address: undefined = undefined;

    // any
    let anyType;

    console.log(serial, userName, isMarried, siblings, siblingsInfo, salaryRange, address, anyType);
}